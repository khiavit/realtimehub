# realtimehub

Proyecto de ejemplo con Maven.

## Compilación

Ejecuta el siguiente comando para compilar el proyecto y ejecutar las pruebas:

```
mvn clean test
```

El artefacto se generará en `target/`.
