pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'realtimehub'
include 'boot', 'api-rest', 'application', 'domain', 'infrastructure'
